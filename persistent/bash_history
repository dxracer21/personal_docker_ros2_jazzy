whoami
hostname
pwd
echo $ROS_DISTRO
echo $ROS_DOMAIN_ID
xclock
hostname -I
ip route | grep default
sudo apt update
sudo apt install -y openssh-server
sudo systemctl enable --now ssh
systemctl is-active ssh
exit
ip route | grep default
exit
xclock
echo $DISPLAY
clear
echo "DISPLAY=$DISPLAY"
echo "XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority}"
xauth list "$DISPLAY"
cd ~/personal_docker_ros2_jazzy
xauth -f .docker.xauth list
exit
rviz2
lsusb
clear
exit
echo $DISPLAY
xclock
exit
clear
rviz2
gazebo
clear
ls
clear
echo "===== ROS ====="
echo $ROS_DISTRO
echo "===== GAZEBO ====="
which gz
gz sim --versions
echo "===== ROS-GZ ====="
ros2 pkg list | grep ros_gz
ls -l /opt/ros/jazzy/bin/ros2
source /opt/ros/jazzy/setup.bash
which ros2
ros2 --help
echo 'source /opt/ros/jazzy/setup.bash' >> ~/.bashrc
source ~/.bashrc
which ros2
echo $ROS_DISTRO
exit
which ros2
echo $ROS_DISTRO
which gz
gz sim --versions
ros2 pkg list | grep ros_gz
exit
ls
cd src
ls
cd..
cd ..
cd src
unzip UAV_Ver62_IsaacSim_Package.zip 
sudo apt update
sudo apt install -y unzip
unzip UAV_Ver62_IsaacSim_Package.zip 
mv UAV_Ver62_IsaacSim_Package/uav_description ./
rm -rf UAV_Ver62_IsaacSim_Package
rm -rf __MACOSX
rm UAV_Ver62_IsaacSim_Package.zip
cd uav_description/
cd ~/ros2_ws/src/uav_description
cat > package.xml <<'EOF'
<?xml version="1.0"?>
<package format="3">
  <name>uav_description</name>
  <version>0.0.1</version>

  <description>UAV Ver62 description package for ROS 2 and Gazebo Sim</description>

  <maintainer email="jinsoo@todo.todo">jinsoo</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>
  <exec_depend>robot_state_publisher</exec_depend>
  <exec_depend>joint_state_publisher</exec_depend>
  <exec_depend>xacro</exec_depend>
  <exec_depend>ros_gz_sim</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
EOF

cat > CMakeLists.txt <<'EOF'
cmake_minimum_required(VERSION 3.8)
project(uav_description)

find_package(ament_cmake REQUIRED)

install(
  DIRECTORY
    launch
    meshes
    urdf
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
EOF

rm -f launch/gazebo.launch
rm -f launch/controller.launch
rm -f launch/display.launch
cat > launch/uav_gazebo.launch.py <<'EOF'
from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription, TimerAction
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch_ros.actions import Node
from launch_ros.substitutions import FindPackageShare
from launch.substitutions import PathJoinSubstitution


def generate_launch_description():
    gazebo_launch = PathJoinSubstitution([
        FindPackageShare('ros_gz_sim'),
        'launch',
        'gz_sim.launch.py',
    ])

    uav_urdf = PathJoinSubstitution([
        FindPackageShare('uav_description'),
        'urdf',
        'uav_ver62.urdf',
    ])

    gazebo = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(gazebo_launch),
        launch_arguments={
            # -r을 넣지 않아 일시정지 상태로 시작
            'gz_args': 'empty.sdf',
        }.items(),
    )

    spawn_uav = Node(
        package='ros_gz_sim',
        executable='create',
        output='screen',
        arguments=[
            '-world', 'empty',
            '-file', uav_urdf,
            '-name', 'uav_ver62',
            '-x', '0.0',
            '-y', '0.0',
            '-z', '1.0',
        ],
    )

    return LaunchDescription([
        gazebo,
        TimerAction(
            period=3.0,
            actions=[spawn_uav],
        ),
    ])
EOF

cd ~/ros2_ws/
colcon build --symlink-install --packages-select uav-description
colcon build --symlink-install --packages-select uav_description
ros2 launch uav_description uav_gazebo.launch.py
source install/setup.
source install/setup.bash
ros2 launch uav_description uav_gazebo.launch.py
clear
ros2 launch uav_description uav_gazebo.launch.py
clear
ros2 launch uav_description uav_gazebo.launch.py
exit
